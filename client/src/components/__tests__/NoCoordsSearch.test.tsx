//TODO Verify if this is needed. 
import React from "react";
import { customRender as render, screen, logRoles } from "../../utils/test-utils";
import NoCoordsSearch from "../NoCoordsSearch";

describe("<NoCoordsSreach />", ()=> {
  test("success", ()=> {
     render(<NoCoordsSearch/>)

  })
    test("error", ()=> {
     render(<NoCoordsSearch/>)

  })


})
