//TODO
import React from "react";
import {
  customRender as render,
  screen,
  logRoles,
} from "../../utils/test-utils";
import Province from "../Province";

describe("<Province />", () => {
  test("success", () => {
    render(<Province />);
  });
  test("error", () => {
    render(<Province />);
  });
});
